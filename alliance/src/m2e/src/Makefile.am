## Process this file with automake to produce Makefile.in

AM_CFLAGS    = -g


bin_PROGRAMS = m2e

m2e_LDADD    = -lMpu -lMlu  \
               -lMcl -lMcp  \
               -lMal -lMap  \
               -lMsl        \
               -lMel -lMgl  \
               -lMhl        \
               -lMvg        \
               -lMmg        \
               -lMlo        \
               -lMph -lMut  \
               -lRcn -lm


m2e_SOURCES  = m2e.c m2e.h

