## Process this file with automake to produce Makefile.in
bin_PROGRAMS = lvx

lvx_LDADD = @LIBS@ \
          -lMlu \
	  -lMcl \
	  -lMal \
	  -lMsl \
	  -lMel -lMgl \
	  -lMhl \
	  -lMvl \
	  -lMlo \
	  -lMut \
	  -lRcn 
	   
lvx_SOURCES = lvx.c
