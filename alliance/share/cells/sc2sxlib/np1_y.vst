entity np1_y is
   port (
      i   : in      bit;
      f   : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end np1_y;

architecture structural of np1_y is
Component inv_x2
   port (
      i   : in      bit;
      nq  : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end component;


begin

u4 : inv_x2
   port map (
      i   => i,
      nq  => f,
      vdd => vdd,
      vss => vss
   );


end structural;
