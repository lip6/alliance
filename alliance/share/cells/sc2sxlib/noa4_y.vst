entity noa4_y is
   port (
      i0  : in      bit;
      i1  : in      bit;
      i2  : in      bit;
      i3  : in      bit;
      f   : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end noa4_y;

architecture structural of noa4_y is
Component na2_x1
   port (
      i0  : in      bit;
      i1  : in      bit;
      nq  : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end component;

Component o3_x2
   port (
      i0  : in      bit;
      i1  : in      bit;
      i2  : in      bit;
      q   : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end component;

signal n1  : bit;

begin

u4 : na2_x1
   port map (
      i0  => i3,
      i1  => n1,
      nq  => f,
      vdd => vdd,
      vss => vss
   );

u5 : o3_x2
   port map (
      i0  => i2,
      i1  => i0,
      i2  => i1,
      q   => n1,
      vdd => vdd,
      vss => vss
   );


end structural;
