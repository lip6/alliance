entity ndrv_y is
   port (
      i   : in      bit;
      f   : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end ndrv_y;

architecture structural of ndrv_y is
Component inv_x4
   port (
      i   : in      bit;
      nq  : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end component;


begin

u4 : inv_x4
   port map (
      i   => i,
      nq  => f,
      vdd => vdd,
      vss => vss
   );


end structural;
