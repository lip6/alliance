entity p1_y is
   port (
      i   : in      bit;
      t   : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end p1_y;

architecture structural of p1_y is

Component buf_x2
   port (
      i  : in      bit;
      nq  : out     bit;
      vdd : in      bit;
      vss : in      bit
 );
end component;

begin

u4 : buf_x2
   port map (
      i  => i,
      nq  => t,
      vdd => vdd,
      vss => vss
   );

end structural;
